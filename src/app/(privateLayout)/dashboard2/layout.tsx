import React from 'react';

const layout = ({children}:any) => {

    return (
        <div>
            <nav>Dashvboard nav  22</nav>
            {children}
        </div>
    );
};

export default layout;
import React from 'react';

const layout = ({children}) => {
    return (
        <div>
            <nav>Dashvboard nav  22</nav>
            {children}
        </div>
    );
};

export default layout;